{% extends 'base.html.twig' %}

{% block title %}inscription - gestion soutenance{% endblock %}

{% block body %}

<div class="container-fluid">
    <div class="row">
        <div class="col-4 offset-4 mt-5 container-reg-con">
            <h1 class='text-center'>Bienvenue 👋</h1>

            {{ form_errors(registrationForm) }}

            {{ form_start(registrationForm) }}
                {{ form_row(registrationForm.email, {
                    label: 'Email',
                    attr: {placeholder: 'Votre email'}
                }) }}
                {{ form_row(registrationForm.plainPassword, {
                    label: 'Mot de passe',
                    attr: {placeholder: 'Votre mot de passe'}
                }) }}
                {# {{ form_row(registrationForm.confirmPassword, {
                    label: 'Confirmation de mot de passe',
                    attr: {placeholder: 'Confirmer votre mot de passe'}
                }) }} #}
                {# {{ form_row(registrationForm.agreeTerms) }} #}

                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary" type="button">Inscrire</button>
                </div>
            {{ form_end(registrationForm) }}
        </div>
    </div>
</div>

{% endblock %}
